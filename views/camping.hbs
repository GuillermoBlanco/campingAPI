<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>MY APP</title>
</head>
<body>
Actualiza el camping.

<form action="/campings/{{camping._id}}" method="POST">
    <label id="name">Name: </label>
    <input type="text" id="name" value="{{camping.name}}" placeholder="{{camping.name}}" name="name">

    <label id="phone">Phone: </label>
    <input type="number" id="phone" value="{{camping.phone}}" placeholder="{{camping.phone}}" name="phone">

    <label id="email">Email: </label>
    <input type="text" id="email" value="{{camping.email}}" placeholder="{{camping.email}}" name="email">

    <label id="address">Address: </label>
    <input type="text" id="address" value="{{camping.address}}" placeholder="{{camping.address}}" name="address">

    <label id="location">Location: </label>
    <input type="text" id="location" value="{{camping.location}}" placeholder="{{camping.location}}" name="location">

    <button type="submit">Submit</button>
</form>
</body>
</html>